import NotificationExamCode from "@/components/notifications/notificationExamCode";
import React from "react";

const Examcode = ({ params }: any) => {
  return <NotificationExamCode params={params} />;
};

export default Examcode;
